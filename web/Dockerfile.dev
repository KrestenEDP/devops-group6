# Development Dockerfile for Vite + React + TypeScript
FROM node:20

WORKDIR /app

# Copy package files first for caching
COPY package*.json ./

# Install all dependencies (dev included)
RUN npm ci

# Copy source code
COPY . .

# Create non-root user (optional security best practice)
RUN useradd -m reactjs
RUN chown -R reactjs:reactjs /app
USER reactjs

# Expose Vite dev server port
EXPOSE 5173

# Run the Vite dev server
CMD ["npm", "run", "dev", "--", "--host"]
